services:
  - type: web
    name: permitvista-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_server:app --host 0.0.0.0 --port $PORT
    # If using Docker, Render will automatically detect and use Dockerfile
    # If using Python buildpack, aptfile will be used automatically

